[package]
name = "aeris-adapter-sdk"
version = "0.1.0"
edition = "2021"
description = "Shared SDK for Aeris WASM adapter plugins"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
